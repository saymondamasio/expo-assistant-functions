    /** ${DESCRIPTION} */
class ${INTERFACE_NAME}Impl : ${INTERFACE_NAME} {
    @AppFunction(isDescribedByKDoc = true)
    override suspend fun ${FUNCTION_NAME}(
        appFunctionContext: AppFunctionContext${PARAMETERS}
    ): String {
        return AppFunctionsModule.invokeFromAppFunction(
            appFunctionContext.context,
            "${FUNCTION_NAME}"${PARAMS_MAP}
        )
    }
}
